// Known product issues currently reproduced by integration tests.
//
// Key: "<test file relative to repo root>::<jest fullName>" — fullName is all
// describe titles plus the test title, joined with single spaces. A key
// without the "::<name>" part covers every failure in the file, including
// suite-level load errors. A key naming a describe block covers every test
// nested under it.
//
// The failures-only reporter flags matching failures as KNOWN ISSUE locally
// and in CI job logs, and bakes the ticket into .jest-run-summary.jsonl so
// Barmin can split Slack reports into known vs UNTRACKED failures. When a
// listed test passes, the reporter nudges you to remove the entry.
//
// Optional "envs" scopes an entry to the environments where the bug
// reproduces: "dev", "bugt", "prod", "local". Omit it when the bug is
// environment-independent. A scoped entry is KNOWN only on the listed
// environments; failing elsewhere reports as UNTRACKED with a pointer back
// to the ticket (a tracked bug reaching a new environment is an event —
// comment on the ticket and widen the list). Unmapped registries resolve to
// "local" and match every entry, so local-platform runs are unaffected. The
// "passed" nudge only fires on listed environments, and Barmin promotes an
// entry to a removal candidate only when it was green on all of them in the
// same run.
//
// Full-line comments only — no trailing comments. File tickets with the
// linear-ticket skill; wire entries with the file-known-issue skill. Never
// use test.failing for a product bug: it hides the failure and turns red
// the moment the bug is fixed.
{
  "tests/job/cronjob-execute.test.ts::a cronjob execute action increments its durable counter": {
    "ticket": "BE-1705",
    "envs": ["prod"],
    "note": "pre-existing orphan schedules starve fresh cronjobs on prod even after fix 51e05bb reached production 2026-08-21 (execute counter timed out in that run); dev green — bugt dropped from scope with the nightly sweep 2026-08-24",
  },
  "tests/job/cronjob-app-deletion.test.ts::deleting an app stops its cronjob from invoking another app": {
    "ticket": "BE-1705",
    "envs": ["prod"],
    "note": "pre-existing orphan schedules starve fresh cronjobs; prod red 2 of 6 nights Aug 12-18 and only one green prod leg since fix 51e05bb shipped 2026-08-21 — bugt dropped from scope with the nightly sweep 2026-08-24",
  },
  "tests/app/templates.test.ts::app templates deploy Template remote build: xmcp-starter": {
    "ticket": "SRE-1700",
    "envs": ["dev"],
    "note": "the remote build wedges in the depot 'exporting to client directory' step and is canceled at the 600s cap; deploy reports 'Build failed with exit code 1'; red on dev 5 of 7 nights Aug 12-18",
  },
  "tests/app/templates.test.ts::app templates deploy Template remote build: mastra-starter": {
    "ticket": "SRE-1700",
    "envs": ["dev"],
    "note": "same depot export stall as xmcp-starter, first seen 2026-08-18 in backend run 32143044580 (build 7wxfxpfbjk); the cap-cancel rate rose 0.1% -> 1.0% of dev builds Aug 16-18, so the ticket was reopened",
  },
  "tests/general/be-1719-shared-staging-key.test.ts::be-1719-concurrent-deploys-of-identical-packages": {
    "ticket": "BE-1719",
    "note": "concurrent deploys of one package share the tmp/<sha256> staging key; R2 throttles per key and the CLI aborts on the first 429 with no retry; red on dev 7 of 7, bugt 6 of 6 and prod 6 of 7 nights Aug 12-18",
  },
  "tests/app/cdn-cache.test.ts::app CDN cache smoke cdn cache honors HTTP semantics, purge, and cache key isolation": {
    "ticket": "EDGE-1994",
    "envs": ["prod"],
    "note": "prod never serves a cached response even pinned to one node — red 7 of 7 nights Aug 12-18 and again in the 2026-08-21 production deploy run; bugt dropped from scope with the nightly sweep 2026-08-24",
  },
}
