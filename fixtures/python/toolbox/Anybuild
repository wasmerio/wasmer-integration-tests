# Generated by Anybuild v0.25.0 — https://anybuild.run
# This file is yours to edit: add steps or override the serve below.

load("//anybuild/tools:python.bzl", "python_build", "python_config", "python_serve")

config = python_config(
    schema = 1,
    commands = {
        "start": "uvicorn src.main:app --host 0.0.0.0 --port $PORT",
    },
    python_framework = "fastapi",
    python_server = "uvicorn",
    asgi_application = "src.main:app",
    python_main_file = "src/main.py",
    python_version = "3.13",
    uv_version = "0.8.15",
)

build = python_build(config)

python_serve(config, build, name = "toolbox")
