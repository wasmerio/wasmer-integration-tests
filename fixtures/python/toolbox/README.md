# Python Toolbox
This project is intended to contain a lot of different bits and bobs which perhaps have broken over time.
Basically enable quick iteration of finding issues in deployed apps, and then ensuring regression safety once they've fixed.

Intent is to create tests which calls endpoints/tries out different things which is setup in this test.
